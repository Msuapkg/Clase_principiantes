const barco = "Barco rojo" ;
const impresora = "Impresora blanca" ;
const puerta = "puerta blanca" ;
console.log ( "el " + barco + " tiene dentro una " + impresora + " y tiene una gran " + puerta )
const frase = `el ${barco} tiene dentro una ${impresora} y tiene una gran ${puerta}`
console.log (frase)
console.log (frase.includes ("rojo"))
console.log (frase.toLowerCase())
console.log (frase.toUpperCase())
console.log (frase.length)


